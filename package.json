{
  "name": "watchmatch",
  "version": "1.0.0",
  "description": "WatchMatch - gemeinsam durch Filme swipen und Matches finden.",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "keywords": [
    "movies",
    "swipe",
    "socket.io",
    "tmdb"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
